# Slack bot tokens (from https://api.slack.com/apps)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-level-token

# Optional: restrict who can use the bot (comma-separated Slack user IDs)
# ALLOWED_USER_IDS=U0123456789

# Optional: custom kiro-cli binary path
# KIRO_CLI_PATH=/usr/local/bin/kiro-cli

# Optional: kiro agent name (defaults to "kiro-assistant")
# KIRO_AGENT=kiro-assistant

# Optional: workspace root for per-thread working directories
# WORKSPACE_ROOT=~/Documents/workspace-kiro-slack

# Optional: default working directory for new sessions
# DEFAULT_CWD=~/Projects
